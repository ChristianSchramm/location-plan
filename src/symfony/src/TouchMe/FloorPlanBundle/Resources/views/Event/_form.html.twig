<div class="row">
  <div class="col-md-4">
    {{ form_start(form) }}
        {{ form_errors(form) }}

        <div class="form-group ">


        {{ form_row(form.title) }}
        </div>

        <div class="form-group">
            {{ form_widget(form.save) }}
        </div>
    {{ form_end(form) }}
  </div>
</div>